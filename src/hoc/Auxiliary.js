// This js is created with the thought of wrapping other UI elements inside it.

// Children is a special property that simply outputs whatever gets entered between the opening and closing tag of this CompositionEvent.
const aux=(props)=>props.children;

// There is an inbuilt react component called React.Fragment which is an alternative for React Aux component

export default aux;